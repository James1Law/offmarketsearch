import { MobileBasketSummary } from "@/features/basket/components/MobileBasketSummary"

export const metadata = { title: "Review & pay — Offline.homes" }

export default function MobileBasketPage() {
  return <MobileBasketSummary />
}
