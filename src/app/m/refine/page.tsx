import { MobileRefinePageClient } from "@/features/refine/components/MobileRefinePageClient"

export const metadata = { title: "Refine your results — Offline.homes" }

export default function MobileRefinePage() {
  return <MobileRefinePageClient />
}
