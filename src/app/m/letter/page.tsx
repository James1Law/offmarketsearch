import { MobileLetterPageClient } from "@/features/letter/components/MobileLetterPageClient"

export const metadata = { title: "Write your letter — LetterDrop" }

export default function MobileLetterPage() {
  return <MobileLetterPageClient />
}
